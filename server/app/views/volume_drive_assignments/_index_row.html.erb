<%- unless @tooltips ; then @tooltips = []; ttflag = true ; else; ttflag = false ;  end %>
<tr class="<%= cycle('odd', 'even') -%>">
  <td nowrap="nowrap" width="5%"><%= link_to 'View', self.send(index_row.class.to_s.underscore + '_path', index_row) %> 
<%- if allow_perm(index_row, ['updater']) %>
 / <%= link_to 'Edit', self.send('edit_' + index_row.class.to_s.underscore + '_path', index_row) %>
<%- end -%></td>
  <td><%= link_to index_row.volume.name, volume_path(index_row.volume) %></td>
  <td><%= link_to index_row.drive.name, drive_path(index_row.drive) %></td>
  <td><%= link_to index_row.assigned_at.to_formatted_s(:long), volume_drive_assignment_path(index_row) %></td>
</tr>

<%= @tooltips if ttflag %>
