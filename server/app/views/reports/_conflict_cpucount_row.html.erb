<%- unless @tooltips ; then @tooltips = []; ttflag = true ; else; ttflag = false ;  end %>
<tr class="<%= cycle('odd', 'even') -%>">
  <td nowrap="nowrap" width="5%">
  <%= link_to 'View', self.send(conflict_cpucount_row.class.to_s.underscore + '_path', conflict_cpucount_row) %> / 
  <%= link_to 'Edit', self.send('edit_' + conflict_cpucount_row.class.to_s.underscore + '_path', conflict_cpucount_row) %>
  <td><%= link_to conflict_cpucount_row.name, self.send(conflict_cpucount_row.class.to_s.downcase + '_path', conflict_cpucount_row) %></td>
  <td><%= conflict_cpucount_row.processor_count %></td>
  <td><%= conflict_cpucount_row.os_processor_count %></td>
  <td><%= conflict_cpucount_row.updated_at.to_s %></td>
</tr>

<%= @tooltips if ttflag %>
