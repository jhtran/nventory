<%- unless @tooltips ; then @tooltips = []; ttflag = true ; else; ttflag = false ;  end %>
<tr class="<%= cycle('odd', 'even') -%>">
  <td nowrap="nowrap" width="5%">
  <%= link_to 'View', self.send(have_not_updated_row.class.to_s.underscore + '_path', have_not_updated_row) %> / 
  <%= link_to 'Edit', self.send('edit_' + have_not_updated_row.class.to_s.underscore + '_path', have_not_updated_row) %>
  <td><%= link_to have_not_updated_row.name, self.send(have_not_updated_row.class.to_s.downcase + '_path', have_not_updated_row) %></td>
  <td><%= have_not_updated_row.updated_at.to_s %></td>
</tr>

<%= @tooltips if ttflag %>
