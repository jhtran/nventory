<%- unless @tooltips ; then @tooltips = []; ttflag = true ; else; ttflag = false ;  end %>
<tr>
  <th nowrap="nowrap" width="5%">&nbsp;</th>
  <th><%= tooltip(Tag,:name) %></th>
  <th><%= tooltip(Tag,:description) %></th>
  <th><%= tooltip(Node,:node_groups) %></th>
</tr>

<%= @tooltips if ttflag %>
