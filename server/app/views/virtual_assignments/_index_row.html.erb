<%- unless @tooltips ; then @tooltips = []; ttflag = true ; else; ttflag = false ;  end %>
<tr class="<%= cycle('odd', 'even') -%>">
  <td nowrap="nowrap" width="5%"><%= link_to 'View', self.send(index_row.class.to_s.underscore + '_path', index_row) %> 
<%- if allow_perm(index_row, ['updater']) %>
 / <%= link_to 'Edit', self.send('edit_' + index_row.class.to_s.underscore + '_path', index_row) %>
<%- end -%></td>
  <td><%= link_to index_row.virtual_host.name, node_path(index_row.virtual_host) %></td>
  <td><%= link_to index_row.virtual_guest.name, node_path(index_row.virtual_guest) %></td>
  <td><%= link_to index_row.assigned_at.to_formatted_s(:long), virtual_assignment_path(index_row) %></td>
</tr>

<%= @tooltips if ttflag %>
