<%- unless @tooltips ; then @tooltips = []; ttflag = true ; else; ttflag = false ;  end %>
<tr>
  <th nowrap="nowrap" width="5%">&nbsp;</th>
  <th <%= sort_td_class_helper "name" %> > <%= sort_link_helper "Name", "name" %></th>
  <th><%= tooltip(Outlet,:producer) %></th>
  <th><%= tooltip(HardwareProfile,:outlet_type) %></th>
  <th><%= tooltip(Outlet,:consumer) %></th>
  <th><%= tooltip(Outlet,:consumer_type) %></th>
</tr>

<%= @tooltips if ttflag %>
